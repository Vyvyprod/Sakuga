# 🎨 Руководство по кастомизации сайта Sakuga

Это подробное руководство поможет вам настроить сайт под свои нужды.

## 📝 Изменение текстового контента

### Название ресторана

**В навигации и футере:**
Откройте `index.html` и найдите:
```html
<h1>SAKUGA</h1>
```
Замените на название вашего ресторана.

**В заголовке страницы (SEO):**
```html
<title>Sakuga - Ресторан азиатской кухни в Якутске</title>
```

### Описание ресторана

Найдите секцию `#about` и измените текст:
```html
<h3>Добро пожаловать в Sakuga</h3>
<p>Мы рады приветствовать вас...</p>
```

### Преимущества (Features)

В секции "О ресторане" есть три блока с иконками:
```html
<div class="feature">
    <div class="feature-icon">🍱</div>
    <h4>Авторская кухня</h4>
    <p>Уникальные рецепты наших шеф-поваров</p>
</div>
```

**Доступные иконки:**
- 🍱 Бенто
- 🍣 Суши
- 🍜 Рамен
- 🥢 Палочки
- 👨‍🍳 Повар
- ⭐ Звезда
- 🏮 Фонарь
- 🎌 Флаг
- 🌸 Сакура

## 🎨 Изменение дизайна

### Цветовая схема

Откройте `css/style.css` и найдите раздел `:root`:

```css
:root {
    --color-primary: #DC143C;      /* Основной цвет (красный) */
    --color-secondary: #000000;    /* Вторичный цвет (черный) */
    --color-white: #FFFFFF;        /* Белый */
    --color-gray-light: #F5F5F5;   /* Светло-серый фон */
    --color-gray: #CCCCCC;         /* Серый */
    --color-gray-dark: #333333;    /* Темно-серый текст */
    --color-red-dark: #B01030;     /* Темно-красный (hover) */
}
```

**Примеры альтернативных цветовых схем:**

**Синяя схема (для суши-бара):**
```css
:root {
    --color-primary: #2E86AB;      /* Морской синий */
    --color-secondary: #06283D;    /* Темно-синий */
    --color-red-dark: #1F5F8B;     /* Темнее для hover */
}
```

**Зеленая схема (эко-стиль):**
```css
:root {
    --color-primary: #4CAF50;      /* Зеленый */
    --color-secondary: #1B5E20;    /* Темно-зеленый */
    --color-red-dark: #388E3C;     /* Темнее для hover */
}
```

**Золотая премиум-схема:**
```css
:root {
    --color-primary: #D4AF37;      /* Золотой */
    --color-secondary: #000000;    /* Черный */
    --color-red-dark: #B8960F;     /* Темное золото */
}
```

### Шрифты

**Текущие шрифты:**
- Заголовки: Playfair Display (serif)
- Основной текст: Montserrat (sans-serif)

**Изменение шрифтов:**

1. Выберите шрифты на [Google Fonts](https://fonts.google.com/)
2. Замените ссылку в `index.html`:
```html
<link href="https://fonts.googleapis.com/css2?family=YOUR_FONT&display=swap" rel="stylesheet">
```
3. Обновите переменные в `css/style.css`:
```css
:root {
    --font-primary: 'YourFont', sans-serif;
    --font-heading: 'YourHeadingFont', serif;
}
```

**Рекомендуемые комбинации:**

*Элегантный стиль:*
- Заголовки: Cormorant Garamond
- Текст: Open Sans

*Современный стиль:*
- Заголовки: Raleway
- Текст: Roboto

*Азиатский стиль:*
- Заголовки: Noto Serif JP
- Текст: Noto Sans JP

### Размеры и отступы

**Размер секций:**
```css
section {
    padding: 80px 0;  /* Вертикальные отступы секций */
}
```

**Размер заголовков:**
```css
.hero-title {
    font-size: 5rem;  /* Главный заголовок */
}

.section-title {
    font-size: 3rem;  /* Заголовки секций */
}
```

## 🖼️ Настройка фонов

### Главная секция (Hero)

**Изменение фонового изображения:**
```css
.hero {
    background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), 
                url('../images/your-image.jpg') center/cover no-repeat;
}
```

**Изменение прозрачности оверлея:**
```css
.hero-overlay {
    background: linear-gradient(
        135deg, 
        rgba(220, 20, 60, 0.3),  /* Первое число - прозрачность (0-1) */
        rgba(0, 0, 0, 0.7)       /* Второе число - прозрачность */
    );
}
```

### Добавление фонов в другие секции

**Пример: фон для секции "О ресторане":**
```css
.about-section {
    background: linear-gradient(rgba(0, 0, 0, 0.8), rgba(0, 0, 0, 0.8)),
                url('../images/about-bg.jpg') center/cover fixed;
    color: white; /* Не забудьте изменить цвет текста */
}
```

**Параллакс эффект:**
```css
.about-section {
    background-attachment: fixed; /* Добавьте эту строку */
}
```

## 📱 Адаптация для мобильных

### Изменение точек останова (breakpoints)

В `css/style.css` найдите медиа-запросы:

```css
@media (max-width: 968px) {
    /* Планшеты */
}

@media (max-width: 768px) {
    /* Мобильные (landscape) */
}

@media (max-width: 480px) {
    /* Мобильные (portrait) */
}
```

### Скрытие элементов на мобильных

```css
@media (max-width: 768px) {
    .desktop-only {
        display: none;
    }
}
```

## 🍽️ Настройка меню

### Добавление новой категории

**Шаг 1:** Добавьте кнопку в `index.html`:
```html
<button class="category-btn" data-category="appetizers">Закуски</button>
```

**Шаг 2:** Добавьте блюда с этой категорией в `data/menu.json`:
```json
{
    "id": 20,
    "name": "Эдамаме",
    "category": "appetizers",
    ...
}
```

### Изменение отображения цен

**Валюта:**
В `js/app.js` найдите:
```javascript
<span class="item-price">${item.price} ₽</span>
```

Замените `₽` на вашу валюту: `$`, `€`, `₸` и т.д.

**Формат цены:**
```javascript
// С разделителями тысяч
<span class="item-price">${item.price.toLocaleString('ru-RU')} ₽</span>

// Две десятичные позиции
<span class="item-price">${item.price.toFixed(2)} ₽</span>
```

## 🎭 Анимации

### Скорость анимаций

В `css/style.css`:
```css
:root {
    --transition: all 0.3s ease; /* Измените 0.3s на желаемую скорость */
}
```

### Отключение анимаций

Для повышения производительности на слабых устройствах:
```css
@media (prefers-reduced-motion: reduce) {
    * {
        animation: none !important;
        transition: none !important;
    }
}
```

### Добавление новых анимаций

**Появление снизу:**
```css
@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.animated-element {
    animation: fadeInUp 0.6s ease;
}
```

## 📧 Настройка формы

### Добавление новых полей

В `index.html`:
```html
<div class="form-group">
    <input type="text" id="company" name="company">
    <label for="company">Название компании</label>
    <span class="form-error"></span>
</div>
```

### Изменение обязательных полей

Удалите атрибут `required` для необязательных полей:
```html
<input type="email" id="email" name="email">  <!-- Необязательное -->
<input type="text" id="name" name="name" required>  <!-- Обязательное -->
```

### Текст успешной отправки

В `index.html`:
```html
<div class="form-success" id="formSuccess">
    Спасибо! Мы свяжемся с вами в течение 24 часов.
</div>
```

## 🌐 SEO оптимизация

### Мета-теги

В `index.html`, в секции `<head>`:
```html
<meta name="description" content="Ваше описание для поисковиков (до 160 символов)">
<meta name="keywords" content="ресторан, азиатская кухня, якутск, суши">
```

### Структурированные данные (Schema.org)

Добавьте в конец `index.html` перед `</body>`:
```html
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Restaurant",
  "name": "Sakuga",
  "image": "https://yoursite.com/images/hero-bg.jpg",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "ул. Ленина, 15",
    "addressLocality": "Якутск",
    "addressCountry": "RU"
  },
  "telephone": "+7-914-123-45-67",
  "servesCuisine": "Азиатская",
  "priceRange": "$$"
}
</script>
```

## 🔧 Дополнительные функции

### Добавление кнопки "Наверх"

**HTML (перед `</body>`):**
```html
<button id="scrollTop" class="scroll-top">↑</button>
```

**CSS:**
```css
.scroll-top {
    position: fixed;
    bottom: 20px;
    right: 20px;
    background: var(--color-primary);
    color: white;
    border: none;
    width: 50px;
    height: 50px;
    border-radius: 50%;
    font-size: 1.5rem;
    cursor: pointer;
    opacity: 0;
    transition: opacity 0.3s;
    z-index: 999;
}

.scroll-top.visible {
    opacity: 1;
}
```

**JavaScript (в `js/app.js`):**
```javascript
const scrollTopBtn = document.getElementById('scrollTop');

window.addEventListener('scroll', () => {
    if (window.scrollY > 300) {
        scrollTopBtn.classList.add('visible');
    } else {
        scrollTopBtn.classList.remove('visible');
    }
});

scrollTopBtn.addEventListener('click', () => {
    window.scrollTo({ top: 0, behavior: 'smooth' });
});
```

### Социальные сети

Замените ссылки в футере:
```html
<div class="footer-social">
    <a href="https://instagram.com/yourrestaurant" class="social-link">Instagram</a>
    <a href="https://vk.com/yourrestaurant" class="social-link">VK</a>
    <a href="https://t.me/yourrestaurant" class="social-link">Telegram</a>
</div>
```

## 💡 Советы по производительности

1. **Оптимизируйте изображения** - используйте формат WebP
2. **Ленивая загрузка** - добавьте `loading="lazy"` к изображениям
3. **Минификация** - сжимайте CSS и JS перед деплоем
4. **CDN** - используйте для статических файлов

---

Если вам нужна помощь с кастомизацией, обращайтесь!
