# 🚀 Руководство по установке и настройке сайта Sakuga

## Шаг 1: Создание структуры папок

Создайте следующие папки для изображений (если они еще не созданы):

```
images/
├── menu/       (для фотографий блюд)
├── drinks/     (для фотографий напитков)
└── gallery/    (для фотографий интерьера ресторана)
```

## Шаг 2: Добавление изображений

### Главный фон (Hero)
Добавьте красивое фото вашего ресторана:
- **Путь**: `images/hero-bg.jpg`
- **Рекомендуемый размер**: 1920x1080 пикселей
- **Формат**: JPG или WebP
- **Совет**: Используйте фото интерьера или красиво сервированного блюда

### Фотографии блюд (Menu)
Добавьте фото ваших блюд в папку `images/menu/`:
- philadelphia.jpg
- california.jpg
- dragon.jpg
- sake-maki.jpg
- spicy-tuna.jpg
- pad-thai.jpg
- tom-yam.jpg
- peking-duck.jpg
- miso.jpg
- ramen.jpg
- wok-seafood.jpg
- kimchi.jpg
- tuna-tataki.jpg
- sunomono.jpg
- chuka.jpg
- mochi.jpg
- matcha-cheesecake.jpg
- fried-icecream.jpg

**Рекомендации**:
- Размер: 800x600 пикселей
- Формат: JPG
- Освещение: Яркое, естественное
- Композиция: Блюдо в центре, красивая подача

### Фотографии напитков (Drinks)
Добавьте фото напитков в папку `images/drinks/`:
- matcha.jpg
- genmaicha.jpg
- puer.jpg
- ginger-tea.jpg
- lychee-lemonade.jpg
- mango-lassi.jpg
- iced-matcha.jpg
- coconut-water.jpg
- sake.jpg
- plum-wine.jpg
- asahi.jpg
- shochu.jpg
- smoothie.jpg
- jasmine.jpg

### Галерея ресторана (Gallery)
Добавьте 9 фотографий интерьера в `images/gallery/`:
- 1.jpg - Общий вид зала
- 2.jpg - Суши-бар
- 3.jpg - Уютный уголок
- 4.jpg - Кухня
- 5.jpg - Вход в ресторан
- 6.jpg - Сервировка стола
- 7.jpg - Детали интерьера
- 8.jpg - Атмосфера вечером
- 9.jpg - VIP-зона (если есть)

**Рекомендации**:
- Размер: 1200x800 пикселей
- Качество: Высокое разрешение
- Стиль: Единая цветовая гамма

## Шаг 3: Настройка контактной информации

Откройте файл `index.html` и найдите секцию с ID `contact`. Замените:

```html
<!-- Найдите и замените следующие строки: -->

<p>г. Якутск, центр города</p>
<!-- На реальный адрес, например: -->
<p>г. Якутск, ул. Ленина, 15</p>

<p>+7 (XXX) XXX-XX-XX</p>
<!-- На реальный телефон: -->
<p>+7 (914) 123-45-67</p>

<p>info@sakuga-restaurant.ru</p>
<!-- На реальный email -->
```

Также обновите время работы:
```html
<p>Ежедневно: 12:00 - 23:00</p>
<!-- На ваше реальное время -->
```

## Шаг 4: Редактирование меню

### Добавление нового блюда

Откройте `data/menu.json` и добавьте новый элемент в массив `items`:

```json
{
  "id": 19,
  "name": "Ваше новое блюдо",
  "category": "hot",
  "description": "Вкусное описание блюда",
  "price": 500,
  "weight": 300,
  "image": "images/menu/your-dish.jpg"
}
```

### Изменение существующего блюда

Просто найдите нужное блюдо по `name` или `id` и измените любые параметры.

### Удаление блюда

Удалите весь объект блюда из массива (не забудьте про запятые между элементами).

## Шаг 5: Настройка цветов (опционально)

Если хотите изменить цветовую схему, откройте `css/style.css` и измените переменные в самом начале:

```css
:root {
    --color-primary: #DC143C;    /* Основной красный */
    --color-secondary: #000000;  /* Черный */
    --color-white: #FFFFFF;      /* Белый */
    
    /* Попробуйте другие сочетания: */
    /* --color-primary: #C41E3A; */ /* Темно-красный */
    /* --color-primary: #E74C3C; */ /* Коралловый */
}
```

## Шаг 6: Подключение формы к серверу

### Вариант 1: PHP backend

Создайте файл `send-form.php`:

```php
<?php
header('Content-Type: application/json');

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $name = htmlspecialchars($_POST['name']);
    $phone = htmlspecialchars($_POST['phone']);
    $email = htmlspecialchars($_POST['email']);
    $message = htmlspecialchars($_POST['message']);
    
    $to = "your-email@sakuga-restaurant.ru";
    $subject = "Новое сообщение с сайта Sakuga";
    $body = "Имя: $name\nТелефон: $phone\nEmail: $email\n\nСообщение:\n$message";
    
    if (mail($to, $subject, $body)) {
        echo json_encode(['success' => true]);
    } else {
        echo json_encode(['success' => false, 'error' => 'Ошибка отправки']);
    }
}
?>
```

Затем в `js/app.js` замените функцию `submitForm()`:

```javascript
async function submitForm(form) {
    const formData = new FormData(form);
    
    try {
        const response = await fetch('send-form.php', {
            method: 'POST',
            body: formData
        });
        
        const result = await response.json();
        
        if (result.success) {
            showSuccessMessage();
            form.reset();
        } else {
            alert('Ошибка отправки. Попробуйте позже.');
        }
    } catch (error) {
        console.error('Ошибка:', error);
        alert('Ошибка отправки. Проверьте подключение.');
    }
}
```

### Вариант 2: FormSpree (без сервера)

1. Зарегистрируйтесь на [formspree.io](https://formspree.io)
2. Получите ваш endpoint
3. Измените форму в `index.html`:

```html
<form id="contactForm" class="contact-form" 
      action="https://formspree.io/f/YOUR_FORM_ID" 
      method="POST">
```

## Шаг 7: Оптимизация изображений

Используйте онлайн-инструменты для сжатия изображений:
- [TinyPNG](https://tinypng.com/) - для PNG и JPG
- [Squoosh](https://squoosh.app/) - универсальный оптимизатор

Рекомендуемые параметры:
- Качество JPG: 80-85%
- Формат: JPG для фото, PNG для логотипов
- WebP для современных браузеров (опционально)

## Шаг 8: Тестирование

### Локальное тестирование

Запустите локальный сервер:

```bash
# Python
python -m http.server 8000

# Node.js
npx live-server

# PHP
php -S localhost:8000
```

### Проверьте:
- ✅ Все изображения загружаются
- ✅ Меню отображается корректно
- ✅ Фильтры работают
- ✅ Форма валидируется
- ✅ Галерея открывается
- ✅ Мобильная версия работает (откройте DevTools, F12)

## Шаг 9: Загрузка на хостинг

### Для обычного хостинга:

1. Подключитесь по FTP (используйте FileZilla)
2. Загрузите все файлы в корневую папку сайта
3. Убедитесь, что `index.html` находится в корне
4. Проверьте права доступа к файлам (обычно 644 для файлов, 755 для папок)

### Для GitHub Pages:

```bash
git init
git add .
git commit -m "Initial commit - Sakuga Restaurant"
git remote add origin https://github.com/yourusername/sakuga.git
git push -u origin main
```

Затем в настройках репозитория включите GitHub Pages.

## Шаг 10: SEO оптимизация

Добавьте мета-теги в `index.html`:

```html
<head>
    <!-- Существующие теги... -->
    
    <!-- Open Graph для соцсетей -->
    <meta property="og:title" content="Sakuga - Ресторан азиатской кухни в Якутске">
    <meta property="og:description" content="Авторская азиатская кухня в центре Якутска">
    <meta property="og:image" content="https://yoursite.com/images/hero-bg.jpg">
    <meta property="og:url" content="https://yoursite.com">
    
    <!-- Для карт Google -->
    <meta name="geo.region" content="RU-SA">
    <meta name="geo.placename" content="Якутск">
</head>
```

## 🎉 Готово!

Ваш сайт готов к использованию. Не забудьте:
- Регулярно обновлять меню
- Добавлять новые фотографии в галерею
- Отвечать на сообщения из формы обратной связи
- Обновлять информацию о специальных предложениях

## 💬 Дополнительные возможности

Если хотите добавить:
- **Онлайн-бронирование столов** - интегрируйте систему бронирования
- **Онлайн-оплату** - подключите платежный шлюз
- **Программу лояльности** - добавьте раздел для постоянных клиентов
- **Блог/Новости** - создайте дополнительную страницу

Обращайтесь за помощью!
